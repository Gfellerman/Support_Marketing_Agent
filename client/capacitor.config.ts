
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.supportmarketingagent.app',
  appName: 'Support Marketing Agent',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
